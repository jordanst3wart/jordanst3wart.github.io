[misc]
startup_banner = ""
clean_on_exit = true

[build]
cmd = "templ generate && rm -rf public && go build -o ./tmp/main"
entrypoint = "./tmp/main"
exclude_dir = ["public"]
include_ext = ["go", "templ"]

[log]
time = false
